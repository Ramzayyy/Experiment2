#include<iostream>
#include<math.h>

using namespace std;

int main()
{
	int x , b;
	cout << "Enter a number: ";
	cin >> x;
	cout << "Multiplication table of " << x << ":" << endl;
		
		for (int i=1; i<=10; i++)
		{
			b=x*i;
			cout << i << " * " << x <<" = " << b << endl;
			
		}
	return 0;	
}
